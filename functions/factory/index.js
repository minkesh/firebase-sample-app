const {getMenuItemInstance} = require('./menu_item');
const {getCategoryInstance} = require('./item_category');
const {getBusinessInstance} = require('./business');
const {getPolicyInstance} = require('./policy');

module.exports = {
    getMenuItemInstance,
    getCategoryInstance,
    getBusinessInstance,
    getPolicyInstance
};